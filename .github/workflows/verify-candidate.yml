name: Governance Authoritative Verifier

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
    inputs:
      repository:
        description: Optional enrolled repository (owner/name)
        required: false
        type: string
      pull_request:
        description: Optional pull request number
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: governance-verifier-controller
  cancel-in-progress: false

jobs:
  verify:
    name: Governance Authoritative Verifier
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout exact verifier registry
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          path: verifier
          fetch-depth: 1
          persist-credentials: false

      - name: Checkout exact Governance verifier
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          repository: markheck-solutions/governance
          ref: a100e7536d4c28bd62b38588deec33969eca63fd
          path: governance
          fetch-depth: 1
          persist-credentials: false

      - name: Set up exact Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: "3.12.13"
          check-latest: false

      - name: Create minimum verifier App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1
        with:
          app-id: ${{ vars.GOVERNANCE_VERIFIER_APP_ID }}
          private-key: ${{ secrets.GOVERNANCE_VERIFIER_APP_PRIVATE_KEY }}
          owner: markheck-solutions
          permission-actions: read
          permission-checks: write
          permission-contents: read
          permission-metadata: read
          permission-pull-requests: read

      - name: Install exact verifier code
        shell: bash
        env:
          GH_TOKEN: ""
          GITHUB_TOKEN: ""
          PYTHONHOME: ""
          PYTHONPATH: ""
        run: |
          set -euo pipefail
          runtime="${RUNNER_TEMP}/governance-verifier-runtime"
          install_source="${RUNNER_TEMP}/governance-verifier-source"
          mkdir -p "$install_source"
          git -C governance archive --format=tar HEAD | tar -xf - -C "$install_source"
          python -m venv "$runtime"
          "$runtime/bin/python" -I -m pip install \
            --disable-pip-version-check \
            --no-deps \
            "$install_source"

      - name: Process enrolled repositories and publish exact-head App checks
        shell: bash
        env:
          GOVERNANCE_VERIFIER_TOKEN: ${{ steps.app-token.outputs.token }}
          DIAGNOSTIC_REPOSITORY: ${{ inputs.repository }}
          DIAGNOSTIC_PULL_REQUEST: ${{ inputs.pull_request }}
          PYTHONHOME: ""
          PYTHONPATH: ""
        run: |
          set -euo pipefail
          runtime="${RUNNER_TEMP}/governance-verifier-runtime"
          args=(
            --registry "${GITHUB_WORKSPACE}/verifier/enrollments.json"
            --output-directory "${RUNNER_TEMP}/governance-verification"
            --governance-sha "a100e7536d4c28bd62b38588deec33969eca63fd"
            --verifier-app-id "${{ vars.GOVERNANCE_VERIFIER_APP_ID }}"
          )
          if [[ -n "${DIAGNOSTIC_REPOSITORY}" ]]; then
            args+=(--repository "${DIAGNOSTIC_REPOSITORY}")
          fi
          if [[ -n "${DIAGNOSTIC_PULL_REQUEST}" ]]; then
            args+=(--pull-request "${DIAGNOSTIC_PULL_REQUEST}")
          fi
          "$runtime/bin/python" -I -m governance_eval.verifier_controller "${args[@]}"

      - name: Upload verifier receipt
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: governance-verifier-receipt-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/governance-verification
          if-no-files-found: warn
          retention-days: 30
